rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }

    function userDoc(uid) {
      return get(/databases/$(database)/documents/users/$(uid));
    }

    function isAdmin() {
      return isSignedIn() && userDoc(request.auth.uid).data.role == "admin";
    }

    match /users/{uid} {
      allow read: if isSignedIn() && request.auth.uid == uid;
      allow write: if isSignedIn() && request.auth.uid == uid;
      allow read, list: if isAdmin();
      allow write: if isAdmin();
    }

    match /workSchedules/{scheduleId} {
      allow read, list: if isSignedIn();
      allow write: if isAdmin();
    }

    match /finance/incomes/{docId} {
      allow read, list, write: if isAdmin();
    }

    match /finance/expenses/{docId} {
      allow read, list, write: if isAdmin();
    }

    match /finance/transfers/{docId} {
      allow read, list, write: if isAdmin();
    }

    match /finance/collaborators/{docId} {
      allow read, list, write: if isAdmin();
    }

    match /finance/collaboratorPayments/{docId} {
      allow read, list, write: if isAdmin();
    }
  }
}
